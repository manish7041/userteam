import { UserHighlitedDirective } from './user-highlited.directive';

describe('UserHighlitedDirective', () => {
  it('should create an instance', () => {
    const directive = new UserHighlitedDirective();
    expect(directive).toBeTruthy();
  });
});
